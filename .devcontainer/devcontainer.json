{
  "name": "GithubActionsDebugger",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "capAdd": ["SYS_ADMIN"],
  "securityOpt": ["apparmor:unconfined"],
  "runArgs": ["--device=/dev/fuse"],
  "postStartCommand": "rc-service sshd start",
  "customizations": {
    "vscode": {
      "settings": {
        "extensions.ignoreRecommendations": true
      }
    }
  }
}
